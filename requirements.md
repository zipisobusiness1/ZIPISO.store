## Packages
framer-motion | Smooth animations for page transitions and hover effects
wouter | Lightweight routing (already in stack, noting for usage)
lucide-react | Icon system (already in stack)
zod | Schema validation (already in stack)

## Notes
Brand Colors: Primary Orange (#ff6a00), Background Cream (#fff5e6)
Cart State: Managed via React Context (client-side persistence recommended)
Auth: Standard JWT/Cookie based via API
Images: Unsplash for placeholders, ready for user uploads
